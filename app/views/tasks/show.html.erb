<div class="bg-pink-100">
    <h2>task navn: <%= @task.name %></h2>
    <ul class="bg-violet-200 padding-10">
        <li><%= link_to "Edit task", edit_task_path(@task) %></li>
        <li>
            <%= 
                link_to "Delete task", task_path(@task), 
                data:{
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                }
            %>
        </li>    
    </ul>
</div>
