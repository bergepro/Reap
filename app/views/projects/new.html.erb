<div class="flex flex-col items-center pt-4">
  <div class="p-1 my-4">
    <h1 class="font-bold">New project:</h1>
    <%= render "form", { client: @client, project: @project } %>
    <%= link_to "back to client", @client, class: "text-gray-600 italic text-sm" %>
  </div>
</div>