<%= 
    link_to "remove ", client_project_membership_path(@client, @project, membership), class: 'text-red-900 border-2 border-black', 
    data:{
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
    }
%>
