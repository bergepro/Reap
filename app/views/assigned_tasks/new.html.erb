<div class="flex flex-col items-center pt-4">
  <div class="max-w-md">
    <div class="p-1 my-4">	
      <h1 class="font-bold my-1">New task:</h1>
      <%= render "form", { client: @client, project: @project, assigned_task: @assigned_task, tasks: @tasks } %>
    </div>
  </div>
  <div class="mt-4">
    <%= 
      link_to "Back to Project", [@client, @project], 
        class: "text-gray-600 italic text-sm text-left" 
    %>  
</div>